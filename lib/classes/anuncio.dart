class Anuncio{
  String title;
  String address;
  String type_timework;
  DateTime createdDate;
  int nro_postulantes;

  Anuncio({
    required this.title, 
    required this.address,
    required this.type_timework, 
    required this.createdDate,
    required this.nro_postulantes
  });
}